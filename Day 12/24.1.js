function Square(a, b, c, d){
    this.a = a;
    this.b = b;
    this.c = c;
    this.d = d;
    }

    // const checker = new Square (a, b, c, d) 
    // console.log(checker)

    Square.prototype.chekeSqure = function () {
        if( a === b && b === c && C === D){
            console.log('squre')
        } else {
            console.log(' not a squre')
        }
    }

    Square ( 5, 5, 5 , 5)